import SwiftUI

// Custom colors
let magenta = Color(red: 1.0, green: 0.0, blue: 0.6)

// Shared gradient
let gradient = LinearGradient(
    colors: [magenta, .cyan],
    startPoint: .leading,
    endPoint: .trailing
)
